spring:
  cloud:
    gcp:
      core:
        enabled: false
      secretmanager:
        enabled: false
#  sql:
#    init:
#      mode: always
  kafka:
    properties:
      schema.registry.url: http://localhost:8081 # dynamically overridden by test-containers
  data:
    redis:
      host: localhost # dynamically overridden by test-containers
      port: 6379 # dynamically overridden by test-containers