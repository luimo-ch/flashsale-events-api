spring:
  cloud:
    gcp:
      core:
        enabled: false
      secretmanager:
        enabled: false
#  sql:
#    init:
#      mode: always
#  kafka:
#    properties:
#      schema.registry.url: ${spring.kafka.properties.schema.registry.url} # dynamically set by test-containers
  data:
    redis:
      host: localhost #{spring.data.redis.host} # dynamically set by test-containers
      port: 6379 #${spring.data.redis.port} # dynamically set by test-containers